[plugin]

name="Mesh Origin Editor"
description="Recenters a MeshInstance3D's mesh vertices to a chosen origin (center, bottom-center, or custom offset). Bakes the transform into the mesh data."
author="uno1982"
version="1.0.0"
script="plugin.gd"
